# Rusongs Platform MVP

Современная энциклопедия русской песни, подготовленная для развёртывания на Vercel. Этот MVP реализует ключевые пользовательские сценарии: обзор каталога песен, просмотр исполнений, сравнение версий, работу с текстами/аккордами и базовый вклад сообщества.

## 🚀 Возможности

- **Главная страница** с героем, подборками и витриной исполнений дня/недели.
- **Каталог песен** (`/songs`) с поиском и фильтрами по тегам.
- **Страница песни** (`/songs/[slug]`) с вкладками: исполнения, текст+аккорды, история и комментарии.
- **Инструменты музыканта**: транспонирование аккордов, метроном, подбор аккордов и печать.
- **Сравнение исполнений** с трей и таблицей характеристик.
- **Коллекции** (`/collections`) и страницы подборок (`/collections/[slug]`).
- **Маркетинговые разделы** (`/discover`, `/community`, `/contribute`) для объяснения ценности платформы.
- **API-маршруты** (`/api/catalog`, `/api/performances`) для интеграций и будущих клиентов.

## 🧱 Архитектура

- **Next.js 14 (App Router)** — SSR/ISR, API-маршруты и серверные компоненты.
- **TypeScript 5**, строгая типизация доменных сущностей.
- **Tailwind CSS + дизайн-токены** для соответствия стилю «современная музыкальная библиотека».
- **React Query + Zustand** — подготовка к клиентским мутациям, сравнение исполнений в состоянии.
- **Фиктивные данные** в `lib/data` имитируют основу PostgreSQL/Prisma до подключения БД.

Дерево 🗂:

```
app/
  layout.tsx           # AppShell + Providers (React Query)
  page.tsx             # Главная
  songs/               # Каталог и страницы песен
  collections/         # Подборки
  (marketing)/         # Discover, Community, Collections landing
  api/                 # Каталог и исполнители (JSON)
components/
  layout/              # Header, Footer, Shell
  domain/              # SongHero, SongTabs, PerformanceCard, инструменты
  ui/                  # Общие UI-компоненты (поиск, мобильное меню)
lib/
  data/                # Типы + семпловые данные + хелперы
  hooks/               # Zustand store (compare tray)
  utils/               # Форматирование
public/images/         # Плейсхолдеры
```

## 🛠️ Локальная разработка

```bash
npm install
npm run dev
```

Приложение доступно на `http://localhost:3000`.

### Скрипты

- `npm run dev` — режим разработки.
- `npm run build` — production-сборка.
- `npm run start` — запуск production-сборки.
- `npm run lint` — проверка ESLint.

## ☁️ Деплой на Vercel

1. Создайте проект на Vercel и подключите этот репозиторий.
2. В разделе **Environment Variables** укажите:
   - `NEXT_PUBLIC_SITE_URL` — публичный URL (напр. `https://rusongs.vercel.app`).
   - (опционально) `DATABASE_URL`, `REDIS_URL`, `NEXTAUTH_SECRET` — для будущей интеграции с БД/аутентификацией.
3. Убедитесь, что команда сборки и запуск — значения по умолчанию (`npm run build`, `npm start`).
4. После деплоя включите предварительный просмотр/ISR: страницы App Router кешируются автоматически.

## 🔭 Дальнейшие шаги

- Подключить PostgreSQL через Prisma и заменить `lib/data` реальными запросами.
- Реализовать NextAuth и роли (user/contributor/moderator).
- Добавить API для приёма заявок, модерации и очередей crawler’а.
- Подключить real-time обновления (SSE/WebSocket) для обсуждений.
- Расширить Storybook и тесты (Jest/Playwright) согласно техдокам.

---

Готово к выкладке на Vercel: код придерживается описанной архитектуры, использует дизайн-токены, предоставляет ключевые сценарии и оставляет чёткий путь к production-версии.
